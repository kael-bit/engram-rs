{
  "name": "engram-rs-mcp",
  "version": "0.9.0",
  "description": "MCP server for engram — hierarchical memory engine for AI agents",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "engram-rs-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "prepublishOnly": "npm run build"
  },
  "keywords": ["mcp", "memory", "ai", "agent", "engram", "llm"],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/kael-bit/engram-rs"
  },
  "files": ["dist", "package.json", "README.md"],
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  }
}
